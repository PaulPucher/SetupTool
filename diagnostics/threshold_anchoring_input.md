# Threshold anchoring input

Input for the next work package (threshold anchoring). Source: this
session's own direct real-data validation run, marked **0.1s
direct-validation run** below to distinguish it from any other floor
tested in this arc. Generated by `diagnostics/inspect_cs_floor_
candidate_validation.py` with the candidate `(min_window_s=0.1,
min_span_rad=0.02)` -- the FINAL floor now in `config/parameters.json`
(`cs_min_window_s=0.1`, `cs_min_slip_angle_span_rad=0.02`,
`cs_max_window_m=53.0`). Mode: `ekf_auto_pacejka`. Statistic:
worst-lap-per-corner, apex_region-substituted (exactly what
`_classify_corner` reads for an apex_3-keyed phase). Deterministic, no
randomness involved in this particular script.

## 0.1s direct-validation run -- worst-lap CSf, sorted ascending

```
[('C9', -0.41), ('C3', -0.333), ('C4', -0.134), ('C14', -0.095), ('C12', -0.041),
 ('C7', -0.037), ('C2', -0.032), ('C1', 0.023), ('C13', 0.039), ('C6', 0.069),
 ('C5', 0.074), ('C8', 0.229), ('C11', 0.425), ('C10', 0.857)]
```

## 0.1s direct-validation run -- worst-lap CSr, sorted ascending

```
[('C4', -0.134), ('C3', -0.065), ('C14', 0.013), ('C13', 0.029), ('C5', 0.04),
 ('C8', 0.052), ('C9', 0.084), ('C1', 0.159), ('C2', 0.293), ('C12', 0.316),
 ('C11', 0.364), ('C7', 0.402), ('C6', 0.601), ('C10', 0.714)]
```

## Stability (worst-lap stability_observed_Nm_per_deg)

**MISSING from this run.** `inspect_cs_floor_candidate_validation.py`
only computes/reports `worst_csf`/`worst_csr` (see its own
`_worst_per_instance_apex_region` function) -- it does not extract a
stability statistic per candidate. The fresh anchoring session will
need to produce this itself (e.g. via `diagnostics/inspect_corner_
distribution.py`, which does report `worst-lap stability` alongside
CSf/CSr, run against the current config).

## Context, for whoever picks this up

- 6 of 14 corners CSf-negative, 2 of 14 CSr-negative -- not a
  wholesale artifact population.
- C4 negative on both axles at the corner (worst-lap) level, but the
  per-lap evidence behind that is thin -- see thesis_notes.md "CS
  validity repair, limitation: C4's own short genuine events are only
  partially detected..." before treating C4 as a clean anchor case.
- C1/C6 read healthy on both axles; C2 healthy-within-noise on front,
  healthy on rear; C3 mixed (front healthy, rear small residual
  negative, both axles' negative reading traces to a single lap --
  same thesis_notes.md entry above).
- Pooled per-sample medians (a different statistic, not this file's
  own worst-lap numbers) stayed positive and increased across the
  board vs the original pre-repair measurement -- thesis_notes.md "CS
  validity repair, part A, Phase 4" entries have the full table.
