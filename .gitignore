# Virtual environment
.venv/

# Python cache
__pycache__/
*.pyc

# Database files
data/

# PyInstaller
dist/
build/
*.spec

# IDE
.idea/
.vscode/settings.json

# OS
Thumbs.db
.DS_Store

# Handover document — local only
HANDOVER.md
generate_handover.py

# Diagnostic script outputs — reproducible, not source
diagnostics/*.txt
diagnostics/*.png
diagnostics/*.json
diagnostics/plots/
diagnostics/plots_step2/

# Manufacturer reference data — proprietary team/car data, local only
docs/car_data/
config/car_data.json
docs/literature/

# Study document — local only, LaTeX conversion pass stays out of the repo
docs/study/

# Raw telemetry exports dropped at repo root (team data, large, local
# only) — GT3_PRC_MLA.txt was the first one found sitting untracked here.
# Two root .txt files are legitimate tracked source, not telemetry:
# excepted explicitly rather than moving them, so this stays a pure
# addition with no path changes elsewhere.
/*.txt
!/channel_list.txt
!/requirements.txt
/*.pdf
